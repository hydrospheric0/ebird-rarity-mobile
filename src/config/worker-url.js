/**
 * Single source of truth for the production Cloudflare Worker base URL.
 * Imported by both src/config/api.js (runtime) and vite.config.js (build).
 */
export const PROD_WORKER_URL = 'https://ebird-rarity-mapper.bartwickel.workers.dev'
